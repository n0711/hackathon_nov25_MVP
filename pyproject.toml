[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "learntwin"
version = "0.0.1"
description = "Mastery estimation (BKT) + next-item recommendations"
requires-python = ">=3.10"
dependencies = []

[tool.ruff]
line-length = 100
